# Orchestrating-script deps only. SGLang itself lives in
# lab4.3/.venv-sglang/, separate from this venv — see README setup.

torch>=2.4
diffusers>=0.36       # for benchmark_baseline.py's diffusers comparison row
transformers>=4.40
accelerate>=0.27
imageio[ffmpeg]>=2.34

# huggingface_hub is pulled in transitively by diffusers; pinning explicitly so
# benchmark_baseline.py's hf_hub-related warning suppressions stay valid.
huggingface_hub>=0.23
