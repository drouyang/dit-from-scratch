torch>=2.4
diffusers>=0.36       # WanPipeline + torch.compile path
transformers>=4.40
accelerate>=0.27
imageio[ffmpeg]>=2.34

# SGLang lives in lab4.3 with its own isolated venv — this lab doesn't
# pull it in.
